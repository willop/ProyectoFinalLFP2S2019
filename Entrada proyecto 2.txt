class MyProgram
{
static void Main(String[] args)
{
int a, b; //Solo declaracion de dos variables enteras
int c = 20, d = 21; //Declaracion de dos variables enteras con asignacion
float precio, altura; //Declaracion de dos variables decimales de tipo float
precio = 42.99f; // tipo decimal
String profesor;
profesor = "Luis Fernando Espino"; // Tipo cadena(entre comillas dobles.)
bool visible = false; // Tipo boolean
char letra1; //Declaracion variable de tipo char
letra1 = 'h'; // Tipo caracter. (entre comillas simples.)
Console.WriteLine("Hola mundo "+c);
}
}